# Diário de Desenvolvimento — 2026-06-09

## Trabalho realizado
- Implementação da função `addContact` em `src/contact.c`.
- Adição de prompts para entrada de `nome` e `telefone` do contato.
- Implementação da função `listContacts` em `src/contact.c`.
- Exibição dos contatos armazenados com número, nome e telefone.
- Identificação de funções ainda pendentes: `removeContact` e `findContact`.
