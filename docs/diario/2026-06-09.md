# Diário de Desenvolvimento — 2026-06-09

## Trabalho realizado
- Implementação da função `addContact` em `src/contact.c`.
- Adição de prompts para entrada de `nome` e `telefone` do contato.
- Implementação da função `listContacts` em `src/contact.c`.
- Exibição dos contatos armazenados com número, nome e telefone.
- Identificação de funções ainda pendentes: `removeContact` e `findContact`.
- Criação do workflow de CI/CD em `.github/workflows/ci.yml` para compilar o projeto e gerar release quando um tag `v*` for criado.
- Realização de commit local com a mensagem `FEAT: inicializar release v1.0.0 e adicionar workflow CI/CD`.
- Criação da tag `v1.0.0` como primeira versão do projeto.

## Decisões de arquitetura
- Definido o uso de PostgreSQL como backend de banco de dados para suportar dados de contatos de forma mais robusta e escalável, mantendo persistência confiável além do formato de arquivo local.
- Decidido integrar Python para tarefas específicas do projeto, aproveitando sua produtividade em scripts de manutenção, migrações, importação/exportação e automações auxiliares.
- Planejada a criação de uma interface gráfica para o projeto, visando melhorar a usabilidade e permitir operação mais intuitiva do gerenciador de contatos além da linha de comando.
- Atualização dos documentos principais (`docs/arquitetura.md`, `docs/modelos-de-dados.md`, `docs/requisitos.md`, `docs/README.md`) para refletir a decisão de PostgreSQL, Python e UI gráfica.
- Extensão do backlog em `docs/product-backlog.md` com o planejamento de sprints, metas e critérios de aceitação ágeis.
- Atualização de `docs/uml.md` com diagrama extendido, estado atual, plano futuro e prazos de sprints.
- Criação de `tests/` para rastreamento de testes, com `tests/README.md` e `tests/2026-06-09-add-list-persist.md` registrando data, comando, saída, erros e status.
